services:
  - type: web
    name: my-custom-connector
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn analysis_api:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
